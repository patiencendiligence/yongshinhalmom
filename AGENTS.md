# Project Custom Instructions

- **CRITICAL FILE PROTECTION**: DO NOT modify or delete `public/assets/zodiac_guardians.png`. This asset is frozen and must remain in its current state as per user request.
- Preferred Gemini model for report generation is `gemini-1.5-flash` for stability, followed by others in the fallback list in `server.ts`.
